<?php
declare(strict_types=1);

/**
 * Componente Reutilizável de Abas do Módulo de Produção.
 * Desativado a pedido do usuário — navegação mantida exclusivamente na Sidebar lateral.
 */
function renderProducaoTabsNav(string $activeTab = 'aderencia-mensal'): void
{
    // Ocultado: navegação realizada exclusivamente através do menu lateral (Sidebar)
}
